import { Box, Typography } from "@mui/material";

const Logo = () => {
	return (
		<Box sx={{ cursor: "pointer" }}>
			<Typography variant="h3" color="primary">
				RB Members
			</Typography>
		</Box>
	);
};

export default Logo;
